<!-- GENERATED by `orion install`. DO NOT HAND-EDIT. Edit .orion/orion.yaml instead. -->
---
name: orion
description: >
  ORION dispatcher. Triggers on any ORION workflow phrase — annotate prd, annotate this prd, prepare prd for roadmap, mark prd phases, prd too large, prd to roadmap, generate roadmap, decompose this prd, roadmap from prd, create epic, new epic, add epic, scaffold epic, create feature, new feature, add feature, scaffold feature, create milestone, new milestone, add milestone, scaffold milestone, design epic, architecture for, architect epic, design architecture, new adr, record decision, log decision, create adr, architecture decision, create story, write story card, add story, story for, new story, harden story, harden, prepare story, certify story, ready for implementation, implement story, implement, code this story, build story, build this, implement, edit file, write code, fix all, check everywhere, update all, rename everywhere, test story, write tests, add tests for, write test, add test, test before implement, done, ready to pr, run gates, verification gates, check gates, review pr, review this branch, code review, review pull request, open pr, create pr, agile pr, open pull request, ship, deploy, ship it, go live, close story, mark done, story complete, mark story done, wrap up story, orion next, what should i implement next, what's ready, what's next, next story, next implementation, standup, daily standup, morning check, what did we do, team status update, plan tomorrow, plan next session, north star, end of day, what should i do next, validate npm package, check npm package, pre-publish check, ready to publish, npm publish check.
  Recognizes any skill name listed in .orion/orion.yaml.skills[].name.
tools: [Read, Write, Edit, Bash, Glob, Grep, Task]
---

# ORION dispatcher

You are routing an ORION skill invocation. Do exactly this:

1. **Parse `$ARGUMENTS`.** The first token is the skill name; the rest are arguments
   to forward. If `$ARGUMENTS` is empty, the skill name is `orion` (the dashboard).

2. **Read `.orion/orion.yaml`.** Find the entry in `skills:` whose `name` matches
   the skill name. If no match, list all `skills[].name` values and suggest the
   closest by edit distance (≤ 3).

3. **Resolve the canonical SKILL.md.** It lives at `.orion/<source>/SKILL.md`,
   where `<source>` is `skills[i].source` from the matching manifest entry.

4. **Honor `hard_block`.** If the manifest entry has `hard_block: [...]`, and any
   listed skill has not passed for this story (check `.orion/state/_replay/`),
   refuse and explain which gate must pass first.

5. **Read the canonical SKILL.md.** Follow every instruction in it, substituting
   the forwarded arguments wherever the skill body references `$ARGUMENTS`.

6. **Log the invocation.** Append one line to `.orion/state/_replay.log`:
   `{ISO8601} {skill-name} {arguments}`
   Also write `.orion/state/_replay/{skill}-{ISO8601-filename-safe}.json`
   with resolved tools, profile, and outcome.

7. **On error**, print a usage hint:
   `Use /orion list to see all skills, or /orion describe <skill> for details.`
