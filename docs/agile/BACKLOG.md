# Backlog — Phase-Assigned Non-Blocking Items

> **What this is:** A lightweight, phase-wise list of known follow-ups and deferrals that are **not blocking** their surrounding work. Each item is a one-line placeholder — **a full STORY.md is written later, only when the item is actually scheduled** into a milestone. Until then it lives here so it isn't forgotten.
>
> **How to use:** When starting a phase, scan its table. Promote an item by running `/new-story` (or `/new-epic` if it's large), then change its Status to the story ID and strike it here.
>
> **Not the same as** the [Phase 4 deferred backlog](epics/phase-4-backlog/README.md) (B-01…B-15) — those are large, business-trigger-gated epics. This file is the running catch-all for smaller, phase-assigned follow-ups discovered during delivery.
>
> **ID scheme:** `BL-NN`, sequential, assigned-phase shown in the table. **Last updated:** 2026-08-31.

---

## Phase 1 — Revenue Strategy (v1.1)

| ID | Item | Domain | Priority | Source / Notes | Status |
|----|------|--------|:--------:|----------------|--------|
| BL-01 | **Socket.io generation progress not delivered on staging.** The granular step-by-step progress bar was cosmetic — the socket connects + subscribes but could deliver **zero `progress` events**. Also observed: **socket reconnect churn at completion** (~4 rapid connect/disconnect cycles). | AI | P2 (non-blocking) | Discovered during PT-09 fix / [EPIC-AI-07](epics/phase-0-mvp/EPIC-AI-07/EPIC.md) staging verification, 2026-07-09. | ✅ **Fixed 2026-08-14.** Root cause confirmed, not just the two hinted-at hypotheses: `generations.service.ts` emits `step:0` synchronously right after creating the DB record — before the HTTP response carrying the generation id can possibly have reached the client, let alone before a fresh socket.io handshake completes. Socket.io rooms don't buffer; `.to(room).emit()` on an empty room drops the event. Server fix: `generation-progress.gateway.ts` now caches the last progress payload per generation and replays it directly to any client that subscribes, regardless of timing (30s TTL past a terminal state) — 6 new tests, `api/tests/infographics/generation-progress.gateway.spec.ts`. Client fix: `useGenerationWebSocket.ts` used to tear down and recreate the **entire socket** every time `generationId` changed — it's set to `null` from 3 separate completion-handling paths in the callers, which is the reconnect churn. Split into two effects: socket lifecycle now depends only on the user (connect once, not per-generation); subscribe/unsubscribe messages travel over the same already-open socket as `generationId` changes. **Live-verified**, not just unit-tested: a real generation showed the client subscribing 10.6s after connecting — the exact race — and the replay delivered `step:0` 2ms after the subscribe ack; all 7 steps arrived in order; 0 disconnects during or after the run. |
| BL-02 | **Model-opacity violation on `/usage`.** The Usage Analytics page shows the raw model id **`ideogram-4`** in the "Cost Breakdown by AI Model" table — user-visible, violating Critical Rule #5 (users should never see model names). | AI / DESIGN | P2 | Found 2026-07-09 during Task 2 §2C live QA (`docs/testing/PHASE_0_HUMAN_QA_CHECKLIST.md`). Map internal model ids → user-facing labels ("Quick Generate" / "Campaign Quality" etc.) on the usage/cost surfaces. | ✅ Fixed 2026-07-11 — PT-11, PR #15 (`client/src/lib/modelLabels.ts`) |
| BL-03 | **`SESSION_SECRET` is a dead variable** — read nowhere in the codebase (`grep` clean); `env.validation.ts` documents it as deliberately absent from the schema. Already pruned from `.env.example` (US-LAUNCH-009). | INFRA | P3 | Found 2026-07-12 during US-LAUNCH-009 env enumeration. | ⚠️ **Staging done** — removed 2026-08-14. **Production still has it** — confirmed present, not removed (out of this pass's scope; production var changes deserve an explicit call, not a housekeeping-sweep default) |
| BL-06 | **Property extraction is hardcoded to `gpt-4o` — the Gemini migration never covered it.** `prompt-extractor.service.ts:104` pins `model: 'gpt-4o'`, while `ai-orchestrator.service.ts:56-57` routes only the *headline* call to `gemini-2.5-flash` for `free/solo/team`. Extraction runs on **every** generation before the headline, so an OpenAI outage or credit exhaustion fails generation for **all** plan tiers — including the tiers believed to have been migrated off OpenAI by PRs #9/#10. Single point of failure that the LLM-routing work did not remove. | AI / INFRA | **P1** | Found 2026-08-06 while trying to verify BL-05 Follow-up B: the local dev key returned `429 You have no credits remaining` and extraction failed outright, no fallback. **Not confirmed for production** — Railway holds its own keys — but the architecture means one provider's billing state can take down all generation. Worth a Task 3 pre-go-live check of OpenAI credit + `GEMINI_API_KEY` on Railway, and a decision on whether extraction should route like the headline does. | ✅ **Fixed 2026-08-14.** Confirmed `GEMINI_API_KEY` is set on both Railway staging and production (the "not confirmed for production" note above is stale) — so this had real impact, not just theoretical. Added `OpenAiService.extractStructuredData(systemPrompt, userPrompt, planTier)`, routing identically to `analyzeProperty()`: Gemini 2.5 Flash for `free/solo/team`, GPT-4o for `brokerage`, GPT-4o fallback when Gemini isn't configured. `prompt-extractor.service.ts` no longer reaches past `OpenAiService` via `(this.openAiService as any).openai` — it resolves `planTier` via a new `resolvePlanTier(organizationId)` helper (mirrors the orchestrator's own lookup, degrades to `''` non-fatally) and calls the new method. 10 new tests: `api/tests/ai-generation/openai.service.extract.spec.ts` (6, routing) + `api/tests/infographics/prompt-extractor.plan-tier.spec.ts` (4, tier resolution). Full Gate 1 green (tsc clean, backend 350/350, client 216/216). |
| BL-05 | **Locale org-default is plumbed but not persisted.** `resolveLocale()` accepts `orgDefault` and its precedence is unit-tested, but nothing populates it — no `Organization.defaultLocale` column, no migration, no settings control. The live chain is override → typed symbol → timezone → passthrough, which works; what's missing is a *settable* default, and with it brokerage-wide consistency. | GEN / AI | P3 (non-blocking) | US-GEN-003 finding **F1**, 2026-08-06. Three options costed (persist on Organization ~3–4h · localStorage first ~1h · delete the rung ~15m) with a recommendation and the reasoning, in [docs/research/2026-08-06-LOCALE-ORG-DEFAULT-OPTIONS.md](../research/2026-08-06-LOCALE-ORG-DEFAULT-OPTIONS.md). **Deliberately not decided** — a Prisma migration is poorly timed while production is still un-deployed behind Task 3. Same doc also logs two adjacent open defects (DEMO_MODE price regex truncating Indian numbers; no sale/rent axis on `PropertyInfo`). | 🔲 Not scheduled |
| BL-08 | **Editable-canvas text boxes wrap when the mapped font falls back to Inter — the "₹1.9 / Cr" regression AC5 targeted is still live.** `canvasState.ts:551` sizes each text element's box width straight from the extraction's measured geometry (`safeW = geo.width * scale`) with no adjustment for the font actually applied. `mapExtractedFont()` and the width calculation are fully independent. Font-mapping itself works correctly (AC1/2/4/6 live-verified) and Inter is the *documented, correct* fallback when an identifier is unrecognized — but Inter's glyphs are wider than whatever provider font the box was originally measured for, so text that fit on one line in the source image wraps to two in the editor. | AI | P2 | Found 2026-08-14 running the new live E2E regression test for US-AI-049 AC5 (`e2e/us-ai-049-font-mapping.spec.ts`) — screenshot evidence in `docs/agile/epics/phase-1-ai-core/EPIC-AI-06/stories/US-AI-049/evidence/`. | ✅ **Fixed 2026-08-15.** Re-measures each text element's width against the font actually resolved by `mapExtractedFont()` (not the raw extraction geometry) using a real 2D canvas context, and widens the box (never narrows) when the measured render is wider than the extraction's original box. Reused `connectLayout.ts`'s existing `createMeasureText()` — generalized to accept an optional `family` override rather than duplicating a second measurer (one implementation, per the US-AI-047/US-AI-050 lesson). tsc clean, backend 350/350, client 216/216, zero regressions. **Live-verified**: `e2e/us-ai-049-font-mapping.spec.ts` re-run confirms `lineCount: 1` — the price renders on one line even while still falling back to Inter (a different generation's font identifier, same fallback path, box now correctly widened to fit it). |
| BL-10 | **`element.crop`'s two implementations disagree on coordinate space.** `canvasTypes.ts:84` documents crop as "original-image pixel coordinates," matching `canvasExport.ts`'s `computeCropSourceRect()` (passes crop.{x,y,width,height} straight to `ctx.drawImage`'s natural-pixel source rect). But `ImageElement.tsx`'s preview (`getCroppedImageStyle()`) computes `scaleX = element.width / crop.width` — which only makes visual sense if `crop.width` is in canvas/box-pixel space, not natural-image pixels. If a crop tool ever wires up `element.crop`, preview and export would render two different crops from the same stored value. | AI / DESIGN | P3 | Found 2026-08-15 while closing US-AI-032 AC5/BL-09 — export parity investigation. **Currently unreachable**: no `CropPanel`/`CropTool` component exists anywhere in the app (grep-confirmed), so nothing ever sets `element.crop` and both branches are dead code today. Worth resolving *before* a crop tool ships, not after. | 🔲 Not scheduled |
| BL-09 | **Editable-canvas export does not match the composed preview at full resolution (US-AI-032 AC5).** Documented, real disagreements between the two renderers: preview (DOM) applies text padding (`px-2 py-1`, `TextElement.tsx`) that export (canvas, `canvasExport.ts`) does not; preview uses `object-fit: contain` for images while export stretches (`ctx.drawImage` with no aspect preservation, `canvasExport.ts:466`); `element.crop` is honoured in preview but ignored in export; export sets `ctx.font` with no web-font-readiness wait. **Two competing export functions also exist** — `canvasExport.ts:20` (native canvas) and `canvasState.ts:423` (html2canvas, whose own comments say it breaks on this codebase's oklch theme) — nobody has confirmed which one the Export button actually calls; that's the first task before fixing anything. | AI / DESIGN | P2 | Surfaced in US-AI-032's own STORY.md ("Export parity — AC5 is real work" section, full mismatch table there) and reconfirmed 2026-08-15 when AC5 was deliberately deferred rather than built alongside AC6 (user's call — genuinely separate, sizeable scope: confirm live export fn, build a pixel-diff harness, fix 4 documented mismatches). | ✅ **Closed 2026-08-15 — was already stale when filed.** Re-investigating to build this found the 4 mismatches were already fixed by `ee64aa5` ("T6 align export rendering with the editor preview — US-AI-032") with 21 unit tests from `fffb9b3` (US-DEPLOY-007's T4), both predating this session. Confirmed via call-chain tracing that only `canvasExport.ts` was ever reachable — `canvasState.ts`'s html2canvas alternative (`exportCanvasAsImage`/`downloadCanvasImage`, ~110 lines) had zero callers anywhere in the app; removed it along with the now-unused `html2canvas` import. Live-verified with a new spec, `e2e/us-ai-032-export-parity.spec.ts` (zero cost — static template, no AI generation): a real template exports a correctly-sized 2160×3840 PNG whose visual layout matches the preview (screenshots in `US-AI-032/evidence/`). One genuine new finding along the way, filed separately as **BL-10** (crop coordinate-space mismatch — currently unreachable, no crop tool exists). Gate 1 green throughout. |
| BL-07 | **Real-photo editable fallback always uses the default template — no photo-aware selection.** `connectLayout.ts`'s `composeFromCanonicalValues` picks `templateId ?? DEFAULT_TEMPLATE_ID` unconditionally. `LayoutPlannerService.planLayout()` (GPT-4o Vision → templateId/scrimSide/palette, built in US-AI-044, 49 tests, DI-registered) would make this photo-aware but is wired to nothing. | AI | P3 | Surfaced 2026-08-14 resolving the US-AI-045 re-scope question (EPIC-AI-06/EPIC.md 2026-08-13 log): US-AI-045 as originally written — planner as the *primary* step of every editable generation — is superseded by extraction-led composition (`88db72d`), confirmed the higher-fidelity default. The planner's only remaining real job is this narrower one: wire it into the real-photo fallback path only (US-AI-051), replacing the fixed default with photo-aware selection. **Deliberately deferred, not built** — user's call, revisit if the real-photo flow's usage/complaints justify it. | 🔲 Not scheduled |
| BL-11 | **`orion run harden` is undeliverable — its SKILL.md points at modules the framework no longer ships.** `.orion/skills/core/harden/SKILL.md` (skill v1.0.0) instructs Steps 1, 2, 4 and 5 to load `.orion/bin/lib/ac-coverage.js` (`parseAcTypes`, `checkCoverage`, `acTypesToTcRows`) and `.orion/bin/lib/story-lock.js` (`computeSha`, `writeLock`, `checkLock`, `findStoryMd`). **`.orion/bin/` does not exist** — not in the repo, not in any of the three `.ai-sdlc-backup/` snapshots, not on `feat/plat/orion-0.6.0-upgrade`, and not inside the globally-installed `@orion-ai/orion@0.7.0` package. Since `orion run harden <id>` dispatches `claude --print /orion harden <id>`, an agent following the protocol literally fails at Step 1. The capability itself survived — `orion lock-status <id>` works — so lock logic moved **into the CLI** and SKILL.md was never updated to match. | INFRA / PLAT | P2 (non-blocking) | Found 2026-08-31 while trying to re-harden the three EPIC-INFRA-02 stories. Contributing factor: **version skew** — `orion migrate` reports `installed: v0.6.1` while the CLI and `.orion/orion.yaml` both declare `0.7.0`, so framework files were likely never refreshed. **`orion upgrade` will NOT fix it** — verified 2026-08-31: it reads `.orion/orion.yaml` (0.7.0), sees latest is 0.7.0, and exits "nothing to do". The 0.6.1 figure comes from a *different* marker that only `migrate` reads. So `.orion/bin/` is not recoverable by any documented command: **v0.7.0 ships a harden SKILL.md referencing modules v0.7.0 does not include.** The fix belongs **upstream in `din-prajapati/orion-ai-sdlc`** — either ship `bin/lib/`, or rewrite the SKILL.md protocol to call the CLI (which already has working lock logic) instead of importing modules. Nothing in this repo can resolve it. **`orion doctor` does not catch this**: it passes 48/48 because it validates that skill *files* exist, never their internal module dependencies. Also reports 10 adapter SHA mismatches (`CLAUDE.md`, `AGENTS.md`, all of `.cursor/`), consistent with the same skew. **Consequence:** all three EPIC-INFRA-02 locks read `SHA match: false` and cannot be refreshed by the documented path, and `implement-story` gates on the lock. | 🔲 Not scheduled |
| BL-12 | **INFRA stories are hardened against only the two default AC types.** `PROJECT_CONTEXT.yaml`'s `ac_coverage.per_domain` configures `AI: [regression, edge-case]` and `PAY: [security, currency-edge]`; every other domain — INFRA included — is commented out, so required coverage falls back to `[happy-path, error-path]`. All three EPIC-INFRA-02 stories pass that trivially, which makes hardening them a rubber stamp. This is the exact weakness the file's own AI comment records ("per_domain was {} until then, meaning AI stories were certified against only [happy-path, error-path]"). Two types would earn their place for storage work: **`rollback`** — US-INFRA-002's AC4/AC5 cover *upload fails*, but not upload succeeding and the DB write then failing, which orphans an R2 object or persists a URL pointing at nothing; and **`concurrent`** — AC2 covers `variations > 1` where *every* upload resolves, while the chat flow sends `variations: 3`, so the untested case is partial failure (two succeed, one fails) and no AC forces a decision on what the customer then gets. | INFRA | P2 | Found 2026-08-31 during the same harden attempt as BL-11. Cheap to fix while the stories are unstarted; both gaps are real holes rather than box-ticking. Blocked in practice by BL-11 — adding types raises the bar but nothing can re-lock against it until harden runs. | 🔲 Not scheduled |
| BL-04 | **Dead payment env read-sites** — `PADDLE_*`, `PAYPAL_*` had read-sites in `server/payments/providers/payment-provider.factory.ts` and `server/routes.ts`'s webhook signature switch (not `ensure-database-url.ts` as originally noted — its `PG*` handling is a real, working fallback, not dead, just currently a no-op since `DATABASE_URL` is always set in prod; left untouched). | INFRA | P3 | Found 2026-07-12 (US-LAUNCH-009). | ✅ **Fixed 2026-08-14** — worse than unused reads: `isProviderAvailable('PADDLE'/'PAYPAL')` could report `true` (env var present) while `getProviderByType()` would still throw (no provider class exists) — a latent "listed as available, throws when used" bug. Both now correctly `return false`, unreachable webhook case removed. 4 new regression tests, `api/tests/payments/payment-provider.factory.spec.ts`. |

---

## Phase 2 — Polish & Self-Serve (v1.2)

| ID | Item | Domain | Priority | Source / Notes | Status |
|----|------|--------|:--------:|----------------|--------|
| _(none yet)_ | | | | | |

---

## Phase 3 — Speed & Batch (v1.3)

| ID | Item | Domain | Priority | Source / Notes | Status |
|----|------|--------|:--------:|----------------|--------|
| _(none yet)_ | | | | | |

---

## Unassigned / Needs triage

| ID | Item | Domain | Priority | Source / Notes | Status |
|----|------|--------|:--------:|----------------|--------|
| BL-24 | **Quick Generate hangs forever on a generation that actually succeeded — it has no fallback if the WebSocket completion event is missed.** `RightSidebar` sets `showResults(true)` **only** inside `handleWebSocketProgress` when `status === "completed"`. It has **zero** poll/`getStatus`/`setInterval` references; `AIChatBox` has **16** — an always-on REST status poll that AI Chat falls back to. So a missed socket event is recoverable in AI Chat and terminal in Quick Generate: the button sits at "Starting…" indefinitely while the server has finished. **Measured 2026-09-05 against the dev database: every generation in the last 72h completed in 19–102s (16 of them, median ~25s), including four while a test sat waiting 5+ minutes for results that never rendered.** **Staging only.** An earlier draft of this entry claimed it reproduced locally too — that was wrong: the local run on 2026-09-05 rendered results for both generations and completed the flow. So the hang has been observed on staging only, which makes a socket-delivery difference between environments the likeliest cause rather than a universal client bug. This is the more prominent of the two generate buttons, and the user's natural response to a hang is to regenerate — paying twice for a design they already have. Related to BL-01 (socket progress not delivered, marked fixed 2026-08-14); that fix added server-side replay for late subscribers but does not give this surface a fallback. | AI / EDIT | **P1** | Found 2026-09-05 while running US-AI-053 Gate 2. I first reported this as "generation is slow" — the database says otherwise; it is a delivery gap, not latency. Caveat: observed under Playwright, so a real browser may hold the socket more reliably; the missing fallback is a fact either way. Fix shape: give RightSidebar the same status poll AIChatBox already has. | 🔲 Not scheduled |
| BL-21 | ✅ **FIXED 2026-09-03 — hook wired into `CanvasEditToolbar`; live verification still pending (see Status).** ~~The editable-compose wait is ~30s and had no elapsed-time affordance — `useComposeProgress` had zero consumers.~~ Measured on staging 2026-09-03 from a real AI Chat design: click → compose request **133 ms**, compose HTTP round trip **29,513 ms**, click → "Editable layers active" **31,580 ms** (a second run: 27,768 ms). So ~30s, of which ~93% is the server round trip. Throughout it, `CanvasEditToolbar` shows a **static** "Separating layers…" — no elapsed time, no reassurance. `US-AI-050` ("progress affordance for the editable compose wait", ✅ Done, AC3 live-verified) built exactly that affordance, and `US-EDIT-009` removed its last two call sites (AIChatBox, RightSidebar) along with the toggle. The hook and its spec still exist; nothing imports them. Also: the client logs **nothing** about compose — a console filter for compose/extract/layer during the whole 30s came back empty, against a project rule that says structured logs on every external call. | EDIT / DESIGN | **P2** | Found 2026-09-03 while measuring the wait for US-EDIT-009's Gate 2. Options: wire `useComposeProgress` into `CanvasEditToolbar` (the affordance already exists and is unit-tested — likely a small change), or delete the hook and its spec and accept a static label. Doing neither leaves a 30-second silent wait on the product's stated paid moat. Note `US-AI-048`'s cache makes repeat composes ~3s, so this hurts first-time users specifically — the ones least able to tell a slow feature from a broken one.<br><br>**Fix applied 2026-09-03:** `useComposeProgress` is wired into `CanvasEditToolbar`. Two flags rather than one — `composeInFlight` starts the clock at the click so elapsed time is the wait the user actually had, while the existing delayed `isExtracting` still decides whether to render a loading state at all, preserving US-AI-048's no-flash-on-cache-hit behaviour. The button shows a compact "Separating layers… 12s", switching at 20s to "Still working… 24s"; the hook's full sentence would have stretched the pill across the canvas, so it moved to the button's `title`, which the hook's contract explicitly permits. Gate 1 green (497 backend + client suites). | ⚠️ **Fixed, not yet live-verified** — five E2E attempts on staging could not reach the compose step: AI Chat's backend validation intermittently refused a prompt that had generated successfully three times (see BL-22). |
| BL-22 | **AI Chat's prompt validator only accepts US addresses and US price formats.** `AIChatBox.tsx:537` (`validatePromptFields`) requires one of: a US street suffix (`st\|ave\|rd\|blvd…`), `City, ST` with a 2-letter state code, or a **5-digit** ZIP. Indian addresses fail: "Shela, Ahmedabad" fails, and a 6-digit PIN fails the 5-digit rule. The street pattern also allows exactly **one** word between number and suffix, so "12 Sardar Patel Road" fails — and so would "123 Martin Luther King Blvd". Price detection wants `$`, `k`/`M`, or a bare number; `Cr` and `Lakh` are not recognised. On failure the user gets "I need a bit more detail to generate your design" with a US example (`"3BR house at 123 Oak St, Austin TX for $450k"`), and **no generation is started**. | AI / DESIGN | **P1** | Found 2026-09-03 writing the Gate 2 E2E spec — two real Indian-format prompts were rejected before a valid one was found. This is a product selling to Indian real-estate agents in ₹ via RazorPay: a correctly-written local listing is turned away at the door, and the guidance shown points further in the wrong direction. Cheap fix: accept 6-digit PINs, Indian locality patterns, `Cr`/`Lakh`/`₹`, and allow multi-word street names.<br><br>**Second finding, 2026-09-03 — the same prompt is not always accepted.** There are *two* rejection paths producing the identical "I need a bit more detail" message: the client regex at `AIChatBox.tsx:673`, and a **backend** validation error at `AIChatBox.tsx:910` whose missing-field list is reverse-engineered from the error string (`errorMessage.includes("address")`). The backend path is extraction/LLM-driven and therefore **non-deterministic**: a prompt that generated successfully twice was rejected on the third identical attempt during E2E runs. From the user's side, retyping the same listing sometimes works and sometimes does not, with no way to tell which gate refused them or why. That is worse than a strict-but-consistent validator, and it also makes any E2E test that generates through AI Chat intermittently flaky.<br><br>**The rejection also cannot say what it wants.** When the backend error names neither field, the client falls back to listing **both** address and price as missing (`missingFromBackend.length > 0 ? missingFromBackend : ["address", "price"]`). Observed 2026-09-03 on a prompt containing an unambiguous address *and* price — the user is told both are missing when neither is. The underlying backend message is only matched on the phrases "missing required fields" / "please provide", so any rejection worded differently is either dropped or shown as this generic pair. Worth checking whether staging's prompt extraction (`prompt-extractor.service.ts`, still pinned to `gpt-4o` — see BL-06) is degrading and surfacing as a validation refusal. | AI / DESIGN | **P1** | Found 2026-09-03 writing the Gate 2 E2E spec — two real Indian-format prompts were rejected before a valid one was found, and that valid one was later rejected too. This is a product selling to Indian real-estate agents in ₹ via RazorPay: a correctly-written local listing is turned away at the door, and the guidance shown points further in the wrong direction. | 🔲 Not scheduled |
| BL-23 | **`loadComposedDesignToCanvas` has the same missing undo-history push.** `US-AI-053` added `pushToHistory` to `loadAiVariationToCanvas`, but its sibling loader still calls `loadCanvas` directly, so pressing "Edit elements" and replacing the canvas with extracted layers remains unundoable. Same one-line shape as the fix that landed; kept out of US-AI-053 so that story stayed reviewable. More broadly, the client has only **2** `pushToHistory` callers in total — undo coverage across canvas mutations has never been audited. | EDIT / AI | P2 | Found 2026-09-03 implementing US-AI-053. | 🔲 Not scheduled |
| BL-16 | **A malformed AC label is invisible to `orion harden` — it is not reported as untyped, it simply vanishes.** Writing `**AC9 [regression]**` with the colon *outside* the bold span instead of inside (`**AC9 [regression]:**`) makes `ac-audit` skip the AC entirely: `ac_count` stayed at 8 with 9 ACs present, `untyped: []`, `missing: []`, `complete: true`, and the lock was written green. Silent in the worst direction — a story certifies as fully covered while its newest AC is unknown to the tooling, and `tc-rows` generates no row for it, so it never reaches a test plan either. Suggested fix: count `**AC<n>` occurrences and error when that exceeds parsed ACs. | INFRA / PLAT | **P2** | Found 2026-09-02 hardening US-EDIT-009 — caught only because the TC row count came back 8 when 9 were expected. Any story whose ACs were hand-written rather than generated by `new-story` may carry the same hole; worth a sweep of existing locks. Fix belongs upstream in `din-prajapati/orion-ai-sdlc`, same repo as BL-11/BL-12. | 🔲 Not scheduled |
| BL-17 | **`orion tc-rows --write` corrupts a Test Cases table with extra columns, and silently drops supplementary rows.** It assumes exactly 6 columns; against a 7-column table (an added "AC" column) it appends a *second* header and shifts every cell one column left, so Scenario text lands in Status. It also writes exactly one row per AC, discarding hand-added rows — US-EDIT-009 lost TC-09/10/11, which covered parts of a three-conjunct branch condition that one row cannot express. Scenario text is regenerated from the AC's truncated first line (e.g. `"regression: and its 9 existing"`), which is not a testable scenario. Status/Finding *are* preserved by TC ID. | INFRA / PLAT | P3 | Found 2026-09-02 re-hardening US-EDIT-009. Workaround in place: that story's table carries a "do not run tc-rows against this table" note. Fix upstream: preserve unknown columns, and rows whose IDs exceed the AC count. | 🔲 Not scheduled |
| BL-18 | **`Template.composedDesign` is dormant — producer gone, consumer unreachable.** US-EDIT-009 removed AI Chat's editable branch, the only code that ever set it, so `CenterCanvas.tsx:93` (`if (template.isAiVariation && template.composedDesign)`) can never be true. Left deliberately: `CenterCanvas.tsx` was outside that story's file scope, and removing a consumer in the same change that removes its producer costs you the ability to revert either cleanly. | EDIT | P3 | Found 2026-09-01 implementing US-EDIT-009 T3. Cheap: delete the field, the branch, and CenterCanvas's now-unused `loadComposedDesignToCanvas` import — or keep it if a future story means to carry composed designs through a Template again. | 🔲 Not scheduled |
| BL-19 | **No test covers the `renderMode` compatibility shim (US-EDIT-009 AC7).** `main.ts` sets `forbidNonWhitelisted: true`, so the generate DTO keeps an ignored `renderMode?: unknown` purely to stop a stale browser tab 400ing after the field was removed. Nothing asserts this: an integration test posting a generate body containing `renderMode` and expecting 200-not-400 would close it. Without one, deleting the shim as "dead code" looks safe and is not. | EDIT / PLAT | P3 | Found 2026-09-01 implementing US-EDIT-009 T4. Delete the field *and* this item together, once no deployed client sends `renderMode`. | 🔲 Not scheduled |
| BL-20 | **No React component test harness exists.** `@testing-library/react` is not a dependency, so no test can render a component and assert what a user sees. US-EDIT-009 hit this twice: AC1 (the toggle is gone) and AC5 (`CanvasEditToolbar` must not claim a fresh template is editable) both fell back to structural proxies plus a manual Gate 2 step. AC5 is the notable one — its own source comment documents a bug that shipped once, and the only automated guard now is that the store field it needed no longer exists. | EDIT / PLAT | **P2** | Found 2026-09-01 implementing US-EDIT-009 T5. Adding a harness is a project-level decision, not a story's call; worth taking before the next canvas-UI story rather than after. | 🔲 Not scheduled |

---

## Adding an item

1. Pick the next `BL-NN`.
2. Add a row under the phase you're assigning it to (or "Unassigned" if unsure).
3. One-liner in **Item**, real source in **Source / Notes**. Do **not** write a STORY.md yet.
4. When scheduled: run `/new-story`, set **Status** to the resulting `US-…` ID, and mark the row done.

*Backlog created: 2026-07-09*
