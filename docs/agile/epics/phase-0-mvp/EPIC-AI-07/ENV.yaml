# Environment variables required by EPIC-AI-07
# No new env vars anticipated — this is a delivery-path fix, not new integration.
# Update this file if root-cause investigation (US-AI-034) reveals a config gap
# (e.g. a socket URL or CORS origin var that differs between local and Railway).

epic: EPIC-AI-07

required: []
