# Environment variables required by EPIC-EDIT-03
epic: EPIC-EDIT-03

required: []

notes:
  - "No new environment variables — pure frontend placement/labeling change against existing state
     (useGenerationPrefs, renderMode) and existing backend behavior (US-LAUNCH-015 compose pipeline,
     unchanged)."
