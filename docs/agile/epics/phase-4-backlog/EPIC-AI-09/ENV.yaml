# Environment variables required by EPIC-AI-09
# Update this file as you discover required env vars during implementation.

epic: EPIC-AI-09

required: []
  # No new env vars expected — this epic reuses the existing Prisma/Infographic
  # storage path and static assets already served from client/public/. Only
  # revisit if a story ends up needing something new (e.g. a fresh Ideogram
  # call for a photo not already covered by SAMPLE_TEMPLATE_PHOTO_PROMPTS.md).
