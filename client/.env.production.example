# ============================================
# CLIENT - PRODUCTION ENVIRONMENT
# ============================================
# Copy to .env.production or .env in client/ on production build.
# Use Razorpay LIVE key only (rzp_live_...).
#
# Note: When building from project root (npm run build), Vite loads env from
# project root. You can put these VITE_* vars in root .env.production instead.
#
# Usage: cp .env.production.example .env.production
# ============================================

# API Configuration (production)
VITE_API_URL=https://your-api.com
VITE_API_BASE=/api/v1
VITE_CLIENT_URL=https://your-app.com

# Payment - Razorpay (LIVE key only)
VITE_RAZORPAY_KEY_ID=rzp_live_YOUR_KEY_ID

# Payment - Stripe (LIVE key - optional)
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_YOUR_KEY
VITE_ENABLE_STRIPE=false

# Feature Flags
VITE_ENABLE_ANALYTICS=true
