# ============================================
# CLIENT - DEVELOPMENT / TEST ENVIRONMENT
# ============================================
# Copy to .env.development or .env in client/ and fill in values.
# Use Razorpay TEST key only (rzp_test_...).
#
# Note: When running from project root (npm run dev), Vite loads env from
# project root. You can put these VITE_* vars in root .env.development instead.
#
# Usage: cp .env.development.example .env.development
# ============================================

# API Configuration (development)
VITE_API_URL=http://localhost:3001
VITE_API_BASE=/api/v1
VITE_CLIENT_URL=http://localhost:5173

# Payment - Razorpay (TEST key only)
VITE_RAZORPAY_KEY_ID=rzp_test_YOUR_KEY_ID

# Payment - Stripe (TEST key - optional)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_YOUR_KEY
VITE_ENABLE_STRIPE=false

# Feature Flags
VITE_ENABLE_ANALYTICS=false
